{
  "collectionName": "components_shared_sitemaps",
  "info": {
    "displayName": "Sitemap",
    "icon": "map",
    "description": "Sitemap settings for this page"
  },
  "options": {},
  "attributes": {
    "showInSitemap": {
      "type": "boolean",
      "default": true,
      "required": true
    },
    "priority": {
      "type": "decimal",
      "default": 0.5,
      "min": 0,
      "max": 1
    },
    "changeFrequency": {
      "type": "enumeration",
      "enum": [
        "always",
        "hourly",
        "daily",
        "weekly",
        "monthly",
        "yearly",
        "never"
      ],
      "default": "monthly"
    }
  }
}
