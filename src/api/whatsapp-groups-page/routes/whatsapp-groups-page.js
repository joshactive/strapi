'use strict';

/**
 * whatsapp-groups-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::whatsapp-groups-page.whatsapp-groups-page');

