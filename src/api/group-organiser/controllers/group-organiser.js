'use strict';

/**
 * group-organiser controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::group-organiser.group-organiser');
